{
  "name": "spotify-obs-overlay",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted Spotify now-playing overlay for OBS browser sources",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "node --env-file-if-exists=.env --watch server/index.js"
  }
}
